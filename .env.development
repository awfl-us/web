# Development build-time environment for Vite
# Leave VITE_API_BASE empty to use the Vite proxy (/api → localhost backend).
# You can override these in .env.local.

VITE_API_BASE=

# By default, require auth in development. You can set this to 1 in .env.local
# if your local backend supports unauthenticated testing.
VITE_SKIP_AUTH=0
