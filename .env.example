# Copy this file to .env.local and fill in values.
# Do not commit .env.local; keep it ignored in Git.
# Vite only exposes variables that start with VITE_.
# Firebase config values are typically public to clients, but still rotate if leaked.

# Backend base URL for API requests. Leave empty to use Vite dev proxy.
# Example for production: https://api.awfl.us
VITE_API_BASE=

VITE_FIREBASE_API_KEY=
VITE_FIREBASE_AUTH_DOMAIN=
VITE_FIREBASE_PROJECT_ID=
VITE_FIREBASE_APP_ID=
VITE_FIREBASE_MESSAGING_SENDER_ID=
VITE_FIREBASE_MEASUREMENT_ID=

# Optional: local dev bypass (backend must allow it)
# 0 = use auth, 1 = skip auth
VITE_SKIP_AUTH=0
